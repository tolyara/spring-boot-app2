package com.springboot.app2.wiki.javacore.multithread;

/**
 *
 * What are the differences between the wait and sleep method in Java?
 * The only major difference is that wait releases the lock or monitor while sleep doesn't release any lock or monitor while waiting.
 * The wait is used for inter-thread communication while sleep is used to introduce pause on execution.
 *
 */
public class WaitVsSleep {
}
